// Definerer en klasse for at kunne oprette objekter med
// samme struktur på tværs af objekter.
export class Pips {
    constructor(username, content, created) {
        this.username = username;
        this.content = content;
        this.created = created;
    }

}